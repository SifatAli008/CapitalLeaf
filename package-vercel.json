{
  "name": "capitalleaf-frontend",
  "version": "1.0.0",
  "description": "CapitalLeaf Frontend - Next.js Application",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run start",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
